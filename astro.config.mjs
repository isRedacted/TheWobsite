// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://wobsite.pages.dev/',
    outDir: 'dist',
    publicDir: 'public'
});